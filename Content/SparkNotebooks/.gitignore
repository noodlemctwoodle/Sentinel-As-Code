# Local config holding the real Log Analytics workspace name. Never commit.
# Copy .env.example to .env and set SENTINEL_WORKSPACE_NAME there instead.
.env

# Jupyter checkpoint dirs from local editing.
.ipynb_checkpoints/
